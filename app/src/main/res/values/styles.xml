<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NewTheme" parent = "Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/Primary</item>
        <item name="colorPrimaryDark">@color/PrimaryDark</item>
        <item name="colorAccent">@color/Accent</item>
        <item name="android:colorBackground">@color/BackGround</item>
    </style>
    <style name="BasicText" parent = "@android:style/TextAppearance">
        <item name="android:textColor">@color/BasicColor</item>
        <item name="android:fontFamily">@font/genr102</item>
    </style>
    <style name="ChampionText" parent = "@android:style/TextAppearance">
        <item name="android:textColor">@color/Champion</item>
        <item name="android:fontFamily">@font/targa</item>
    </style>
</resources>
